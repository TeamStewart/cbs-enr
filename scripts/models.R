# ============================
# Function: execute models
# ============================
run_models <- function(data, state, county, timestamp, history) {
  
  dir_create(glue("{PATH_DROPBOX}/24_general/{state}/model_archive"))
  
  


  
}